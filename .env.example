# Copy this file to .env and fill in real values. .env itself is gitignored
# and never committed — this .example file is the one that's tracked in git,
# so anyone cloning the repo knows what secrets they need to provide.

ROBOFLOW_API_KEY=
